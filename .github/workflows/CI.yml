name: Format + Lint + Tests

on:
    push:
        branches:
            - main
    pull_request:

jobs:
    ci:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                python-version: ["3.13"]
        
        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                python-version: ${{ matrix.python-version }}

            - name: Install dependencies
              run: |
                   python -m pip install --upgrade pip
                   pip install -r requirements.txt
                   #pip install black flake8 pytest pytest-cov

            - name: Run Black
              run: black --check .

            - name: Run Mypy
              run: mypy .

            - name: Run Flake8
              run: flake8 . --exclude .venv --ignore=E501 # ignores line too long warning (conflicts with formatter)

            - name: Run unit tests
              run: python -m pytest tests/ -v --cov=. --cov-fail-under=85
